import { FC } from 'react'
interface useCallbackProps {
  
}
const useCallback: FC<useCallbackProps> = ({
  
}) => {
  return (
    <div>
      useCallback
    </div>
  )
}

export default useCallback