import { useCallback, useRef } from "react"
import useMemoProp from "./hooks/useMemo"
import useContext from "./hooks/useContext"
import useReducerComp from "./hooks/useReducer"


const App = () => {
  return (
    <div>App</div>
  )
}

export default App